__pycache__/
*.py[cod]
.venv/
.env
.vscode/
.claude/
config.yml
.lastfm_session
dev_out/
test_records/
lp/cache/
# Pre-rendered vinyl caches (mandelbrot/nebula/julia/munafo). Runtime-generated;
# the munafo snapshots are regenerated from the bongsweat repo. (Note: these are
# currently untracked — see the prior !lp/cache/munafo negation which never took,
# since git can't re-include inside an ignored parent.)
lpcore/cache/
.lp_state.json
.lp_launch.json
image.png
