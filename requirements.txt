# lpcore + lp (kiosk / web UI)
python-vlc
pyyaml
mutagen
fastapi
uvicorn[standard]
pygame-ce
numpy
pylast

# lp-deck + lp-studio (desktop Qt apps); lpcore and lp never import Qt
PySide6>=6.11

# lp-deck: MPRIS2 / media keys (optional at runtime, degrades to a no-op)
dbus-next
