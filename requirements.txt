# lpcore + lp (the kiosk / web UI). This file is what a kiosk install needs and
# nothing more: lpcore and lp never import Qt, and an ODROID/Pi should not be
# made to build PySide6 to play records. The desktop apps add their own deps in
# requirements-deck.txt.
python-vlc
pyyaml
mutagen
fastapi
uvicorn[standard]
pygame-ce
numpy
pylast
