pytest
ruff

# Browser tests (tests/test_web_hold.py). Uses the installed Google Chrome via
# channel="chrome", so `playwright install` is NOT needed. Without playwright or
# Chrome the browser tests skip rather than fail.
playwright
