pytest
ruff

# Browser tests (tests/test_web_*.py). They use the system Google Chrome when it
# is installed; on a machine without it, run `python -m playwright install
# chromium` once. Without either, those tests skip rather than fail.
playwright
