"""lp-deck — a full desktop music player (PySide6) built on lpcore.

Library / playlists / track-level playback with the lp spinning vinyl as the
Now-Playing view. Shares lpcore (player, library, scrobbler, vinyl renderer)
with the lp kiosk; adds a SQLite-backed library index, playlists, a queue, and
per-artist / per-album vinyl overrides.
"""
__version__ = "0.0.1-dev"
